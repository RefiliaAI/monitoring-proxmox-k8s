apiVersion: v1
kind: Service
metadata:
  name: monitoring-dashboard
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: monitoring-dashboard
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
